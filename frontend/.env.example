# WebSocket server URL (used in production, e.g. Vercel -> Render).
# Leave unset for local development (falls back to the current host).
# Example: https://satellite-ws-server.onrender.com
VITE_WS_URL=